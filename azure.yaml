name: task-orchestrator
metadata:
  template: task-orchestrator@0.2.0

infra:
  provider: bicep
  path: infra
  module: main

services:
  frontend:
    project: ./frontend
    language: python
    host: containerapp
    docker:
      path: Dockerfile
      context: .
