name: vm-hosted-agent
metadata:
  template: vm-hosted-agent@0.1.0

infra:
  provider: bicep
  path: infra
  module: main

services:
  frontend:
    project: ./frontend
    language: python
    host: containerapp
    docker:
      path: Dockerfile
      context: .
