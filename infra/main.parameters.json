{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "namePrefix": { "value": "${AZURE_NAME_PREFIX=vmagent}" },
    "location": { "value": "${AZURE_LOCATION}" },
    "targetSubscriptionId": { "value": "${VM_TARGET_SUBSCRIPTION_ID=${AZURE_SUBSCRIPTION_ID}}" },
    "foundryProjectEndpoint": { "value": "${FOUNDRY_PROJECT_ENDPOINT}" },
    "hostedAgentName": { "value": "${HOSTED_AGENT_NAME=vmagent-agent-windows}" }
  }
}

